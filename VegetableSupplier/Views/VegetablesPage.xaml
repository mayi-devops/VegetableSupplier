<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="VegetableSupplier.Views.VegetablesPage"
             Title="{x:Static resx:AppResources.Vegetables}">
    <Grid RowDefinitions="Auto,*" Padding="20">
        <!-- Search Bar -->
        <SearchBar Placeholder="{x:Static resx:AppResources.SearchVegetables}"
                   Text="{Binding SearchQuery}"
                   SearchCommand="{Binding SearchCommand}"/>

        <!-- Vegetables List -->
        <RefreshView Grid.Row="1"
                    Command="{Binding RefreshCommand}"
                    IsRefreshing="{Binding IsRefreshing}">
            <CollectionView ItemsSource="{Binding Vegetables}"
                          EmptyView="{x:Static resx:AppResources.NoVegetablesFound}">
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <SwipeView>
                            <SwipeView.RightItems>
                                <SwipeItems>
                                    <SwipeItem Text="{x:Static resx:AppResources.Delete}"
                                              BackgroundColor="Red"
                                              Command="{Binding Source={RelativeSource AncestorType={x:Type views:VegetablesPage}}, Path=BindingContext.DeleteVegetableCommand}"
                                              CommandParameter="{Binding .}"/>
                                </SwipeItems>
                            </SwipeView.RightItems>
                            
                            <Frame Margin="0,5" Padding="15">
                                <Frame.GestureRecognizers>
                                    <TapGestureRecognizer 
                                        Command="{Binding Source={RelativeSource AncestorType={x:Type views:VegetablesPage}}, Path=BindingContext.EditVegetableCommand}"
                                        CommandParameter="{Binding .}"/>
                                </Frame.GestureRecognizers>
                                
                                <Grid ColumnDefinitions="*,Auto">
                                    <VerticalStackLayout>
                                        <Label Text="{Binding Name}"
                                               FontAttributes="Bold"/>
                                               
                                        <Label Text="{Binding Unit}"
                                               TextColor="{StaticResource Gray500}"/>
                                    </VerticalStackLayout>
                                    
                                    <Label Grid.Column="1"
                                           Text="{Binding Price, StringFormat='₹{0:N2}'}"
                                           VerticalOptions="Center"
                                           FontAttributes="Bold"/>
                                </Grid>
                            </Frame>
                        </SwipeView>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </RefreshView>

        <!-- Add Vegetable Button -->
        <Button Grid.Row="1"
                Text="+"
                FontSize="32"
                WidthRequest="60"
                HeightRequest="60"
                CornerRadius="30"
                HorizontalOptions="End"
                VerticalOptions="End"
                Margin="20"
                Command="{Binding AddVegetableCommand}"/>
    </Grid>
</ContentPage>